# CtrlZTree Test Workspace

This workspace is opened by `@vscode/test-cli` during extension tests.

It intentionally has its own settings so tests do not read the developer's
normal VS Code profile, installed extensions, update state, or workspace trust
state.
